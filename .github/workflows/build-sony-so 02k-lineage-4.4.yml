name: Build Sony SO-02K Lineage 17.1 Kernel

on:
  workflow_call:
  workflow_dispatch:

env:
  # 设备基础信息
  DEVICE_NAME: "sony_xz1c"
  DEVICE_CODENAME: "so-02k"
  DEFCONFIG_NAME: "so02k_defconfig"
  ARCH: arm64

  # Clang 编译配置（纯LLVM编译，关闭GCC）
  CLANG_TARGET_TRIPLE: "aarch64-linux-android"
  CLANG_TRIPLE: "aarch64-linux-android"
  EXTRA_CMDS: "LD=ld.lld LLVM=1 LLVM_IAS=1 ARCH=arm64 SUBARCH=arm64 KBUILD_CFLAGS='-Wno-incompatible-pointer-types -Wno-unused-parameter' -j$(nproc)"

  # 内核源码仓库
  KERNEL_SOURCE: "https://github.com/whatawurst/android_kernel_sony_msm8998.git"
  KERNEL_BRANCH: "lineage-17.1"

  # Clang 工具链
  CLANG_SOURCE: "https://github.com/llvm/llvm-project/releases/download/llvmorg-19.1.7/LLVM-19.1.7-Linux-X64.tar.xz"
  CLANG_BRANCH: ""

  # GCC 完全关闭，保留变量仅做兼容占位
  GCC_GNU: false
  GCC_64_SOURCE: ""
  GCC_64_BRANCH: ""
  GCC_32_SOURCE: ""
  GCC_32_BRANCH: ""

  # Defconfig 自定义配置（未使用）
  DEFCONFIG_SOURCE: ""
  DEFCONFIG_ORIGIN_IMAGE: ""
  DEFCONFIG_FROM_BOOT: false

  # 完全禁用 KernelSU，关闭所有相关逻辑
  KERNELSU_SOURCE: ""
  KERNELSU_BRANCH: ""
  KERNELSU_AUTO_GET: "false"
  KERNELSU_AUTO_FORK: "resukisu"
  KERNELSU_METHOD: "shell"

  # SUSFS 全局关闭，屏蔽补丁步骤
  SUSFS_ENABLE: false
  SUSFS_FIXED: false
  SUSFS_FOLDER_FIXED: ""
  SUSFS_PATCH_FIXED: ""

  # AnyKernel3 打包配置
  AK3_SOURCE: "https://github.com/osm0sis/AnyKernel3.git"
  AK3_BRANCH: "master"
  PACK_METHOD: "Anykernel3"
  PACK_KMODULES: "false"

  # DTBO / Boot 镜像相关（未启用）
  BOOT_SOURCE: ""
  NEED_DTBO: false
  NEED_SAFE_DTBO: false
  ROM_TEXT: "rom_android"
  PYTHON_VERSION: "3"

  # 补丁功能开关
  PATCHES_SOURCE: ""
  PATCHES_BRANCH: ""
  REKERNEL_ENABLE: "true"
  DROIDSPACES_ENABLE: "false"
  HOOK_METHOD: "syscall"
  KERNEL_PATCH: "false"
  KPM_ENABLE: "false"
  KPM_INLINE: "false"
  KPM_KPATCH_NEXT: "false"
  KPM_PATCH_SOURCE: "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU_patch/refs/heads/main/kpm/patch_linux"
  BASEBAND_GUARD_ENABLE: "false"
  BASEBAND_GUARD_BOOT: "false"

  # 编译附加参数
  GENERATE_DTB: "false"
  GENERATE_CHIP: "qcom"
  BUILD_DEBUGGER: "false"
  SKIP_PATCH: "false"
  BUILD_OTHER_CONFIG: "false"
  MERGE_CONFIG_FILES: "vendor/debugfs.config,vendor/lahaina_QGKI.config"
  FREE_MORE_SPACE: "false"

  # CCache 缓存优化
  CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
  CCACHE_NOHASHDIR: "true"
  CCACHE_HARDLINK: "true"

jobs:
  build-kernel:
    name: Build Kernel
    runs-on: ubuntu-latest
    steps:
      - name: ⏰ Checkout Workflow Repository
        uses: actions/checkout@v6

      - name: ⚙️ Initialize Build Environment
        uses: ./.github/workflows/build-env

      - name: 📦 Pull Kernel Source & AnyKernel3
        uses: ./.github/workflows/build-ready

      # 全局深度源码清理，解决dirty源码编译报错
      - name: 🧹 Full Source Deep Clean
        run: |
          cd $GITHUB_WORKSPACE/device_kernel
          git clean -fdx
          make mrproper
          rm -rf out .config include/config generated arch/*/include/generated
          rm -rf $GITHUB_WORKSPACE/clang-custom $GITHUB_WORKSPACE/gcc-64 $GITHUB_WORKSPACE/gcc-32

      # SUSFS 已全局关闭，注释补丁步骤，不再执行
      # - name: 🪝 SUSFS Patch
      #   uses: ./.github/workflows/patch-susfs

      - name: 🔗 Apply No-kprobe Patch
        uses: ./.github/workflows/patch-no-kprobe

      # 无效空步骤直接删除，无执行逻辑
      # - name: 🛠️ Extra kernel opts
      #   run: cd $GITHUB_WORKSPACE/device_kernel

      - name: 🧰 Apply ReKernel Patch
        if: env.REKERNEL_ENABLE == 'true'
        uses: ./.github/workflows/patch-rekernel

      # DroidSpaces 关闭，跳过补丁执行
      # - name: 💿 Droidspaces patch
      #   uses: ./.github/workflows/patch-droidspaces

      # Baseband Guard 关闭，跳过补丁执行
      # - name: 🔒 Baseband guard patch
      #   uses: ./.github/workflows/patch-baseband-guard

      # 预生成defconfig，移除KSU配置引用，固定CPU核心数
      - name: ⚙️ Generate Defconfig & Modify CPU Count
        run: |
          cd $GITHUB_WORKSPACE/device_kernel
          # 删除内核中KernelSU Kconfig引用，规避缺失报错
          sed -i '/source "drivers\/kernelsu\/Kconfig"/d' drivers/Kconfig
          make ARCH=${{ env.ARCH }} ${{ env.DEFCONFIG_NAME }}
          # 固定CPU核心数为16
          sed -i 's/CONFIG_NR_CPUS=.*/CONFIG_NR_CPUS=16/' .config
          make ARCH=${{ env.ARCH }} olddefconfig

      # 编译前二次清理输出目录
      - name: 🧹 Pre-build Output Clean
        run: |
          cd $GITHUB_WORKSPACE/device_kernel
          make mrproper
          rm -rf out

      # 内核编译：纯Clang LLVM编译，清空GCC交叉变量，关闭dirty校验
      - name: 🚀 Start Kernel Compile
        uses: ./.github/workflows/build-process
        env:
          CLANG_TARGET_TRIPLE: ${{ env.CLANG_TARGET_TRIPLE }}
          CLANG_TRIPLE: ${{ env.CLANG_TRIPLE }}
          EXTRA_BUILD_ARGS: "CLANG_TARGET_TRIPLE=${{ env.CLANG_TARGET_TRIPLE }} CLANG_TRIPLE=${{ env.CLANG_TRIPLE }} ${{ env.EXTRA_CMDS }}"
          GCC_64: ""
          GCC_32: ""
          KBUILD_OUTPUT_CHECK: "0"

      # AnyKernel3 打包内核刷机包
      - name: 🔩 Package Kernel With AnyKernel3
        uses: ./.github/workflows/pack-process

      # 上传编译成品压缩包至Action产物
      - name: ✏️ Upload Flashable Zip Artifact
        uses: ./.github/workflows/upload-artifacts